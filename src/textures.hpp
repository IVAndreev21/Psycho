#pragma once
#include "pch.hpp"

class Textures
{
private:
public:
	void InitialiseTextures();
	sf::Texture logo;
	sf::Texture carbon;
	sf::Texture molecules;
	sf::Texture ptable;
	sf::Texture reactions;
	sf::Texture settings;
};